= provide(:title, "Create New Conference")
%h1 Create New Conference
.row
  .container
    .small-12.medium-6.offset3
      = form_for(:conference, url: conferences_path) do |f|
        = f.label :name
        = f.text_field :name
      %p
        #{link_to "Create another conference!", new_conference_path}